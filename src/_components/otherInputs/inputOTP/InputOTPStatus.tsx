import { OTPProps } from "antd/es/input/OTP";

interface ThisProps {
    disabled: boolean;
    fieldStatus: OTPProps["status"] | undefined;
};

export const InputOTPStatus: React.FC<ThisProps> = ({}) => {
    return (
        <></>
    );
};